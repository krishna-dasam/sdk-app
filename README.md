## Run
Requires JDK 17+ and Maven (or use the wrapper).
```bash
./mvnw spring-boot:run
```
Open [http://localhost:8080](http://localhost:8080). Files are stored in the `uploads/` directory at the project root.
